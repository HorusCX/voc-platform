Run the Playwright E2E tests for the VoC platform.

1. Run the tests:
   ```
   cd frontend && npx playwright test
   ```

2. If tests fail, analyze the failures and report what went wrong. Check the playwright-report/ directory for detailed results.
